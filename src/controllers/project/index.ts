import { default as create } from './create';
import { default as getAll } from './getAll';
import { default as getOne, getProjectMiddleware } from './getOne';

export default {
  create,
  getAll,
  getOne,
  getProjectMiddleware,
};
